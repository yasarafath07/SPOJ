#include<bits/stdc++.h>
using namespace std;

int main(){
    ios :: sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int n;

    while(true){
        cin>>n;

        if(n == 42){
            break;
        }
        cout<<n<<endl;
    }

    return 0;
}
